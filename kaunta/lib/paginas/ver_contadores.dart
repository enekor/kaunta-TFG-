import 'package:flutter/widgets.dart';

class VerContadores extends StatelessWidget {
  const VerContadores({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Ver contadores"),
    );
  }
}
